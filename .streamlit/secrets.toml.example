# Copy this into Streamlit Cloud: App → Settings → Secrets
# Use the External Database URL from Render (must match your active Postgres instance).

DATABASE_URL = "postgresql://USER:PASSWORD@HOST/DATABASE"
